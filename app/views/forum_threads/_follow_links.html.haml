- if user_signed_in?
  - if current_user.follows?(@forum_thread)
    = link_to 'Unfollow Thread', forum_thread_follow_path(@forum_thread), method: :delete, class: 'btn-sm btn-danger'
  - else
    = link_to 'Follow Thread', forum_thread_follow_path(@forum_thread), method: :post, data: {confirm: 'Are you sure you want sign up for email updats on this thread?'}, class: 'btn-sm btn-success'
