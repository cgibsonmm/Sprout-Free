- if user_signed_in?
  #user-info-area
    .user-gravatar
      = image_tag current_user.gravatar_url
    .user-info.mt-2
      .username
        = current_user.username
      .user-stats
        Post Count:
        = current_user.forum_posts.all.count
