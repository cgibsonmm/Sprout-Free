$(document).on('turbolinks:load', function(){
  if(gon.admin){
    $('#subjects').sortable({
      update: function(e, ui){
        Rails.ajax({
          url: $(this).data('url'),
          type: "PATCH",
          data: $(this).sortable('serialize')
        })
      }
    })
  }
});
